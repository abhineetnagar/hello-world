#include<stdio.h>
#include<math.h>
int main()
{
  float square(float a);
  float a,b;
  printf("Enter the number :");
  scanf("%f",&a);
  b=square(a);
  printf("Square of %f is %f",a,b);
  return 0;
}
float square(float x)
{
  float y;
  y=x*x;
  return(y);
}
